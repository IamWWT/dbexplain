
▸ Instances (6)
  video-redis                     redis  1 db(s), 1 tables
  aiops-sqlite                    sqlite  1 db(s), 14 tables
  aiops-mysql                     mysql  1 db(s), 2 tables
  openim-redis                    redis  1 db(s), 46 tables
  video-pg                        postgres  1 db(s), 5 tables
  aiops-clickhouse                clickhouse  2 db(s), 6 tables

▸ video-redis  /  db0

  _server_info  Redis 7.4.7 | memory=1.08M | total_keys=
────────────────────────────────────────────────────────────────────────
    (no columns)

▸ aiops-sqlite  /  /home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db

  column_docs ~95 rows
────────────────────────────────────────────────────────────────────────
  name         type  flags         comment
  ───────────  ────  ────────────  ────────────────────
  table_name   TEXT  PK            名称
  column_name  TEXT  PK            名称
  description  TEXT  NN            IP 地址
  indexes: UNI(table_name,column_name)

  conversation_steps
────────────────────────────────────────────────────────────────────────
  name             type     flags         comment
  ───────────────  ───────  ────────────  ────────────────────
  step_id          TEXT     PK            
  intent_id        TEXT     NN            
  step_order       INTEGER                
  step_type        TEXT                   
  slot_name        TEXT                   
  prompt_template  TEXT                   
  next_step_id     TEXT                   
  indexes: UNI(step_id)

  domain_tags ~37 rows
────────────────────────────────────────────────────────────────────────
  name         type     flags         comment
  ───────────  ───────  ────────────  ────────────────────
  tag_path     TEXT     PK            示例: IT
  parent_path  TEXT                   
  name_en      TEXT     NN            名称
  name_zh      TEXT     NN            名称
  level        INTEGER                示例: 0
  domain       TEXT     NN            示例: RESOURCE
  description  TEXT                   IP 地址
  indexes: UNI(tag_path)

  failure_log ~6 rows
────────────────────────────────────────────────────────────────────────
  name          type       flags         comment
  ────────────  ─────────  ────────────  ────────────────────
  id            INTEGER    PK            标识符
  session_id    TEXT                     标识符
  raw_input     TEXT                     示例: tsf consul 怎么运…
  context_json  TEXT                     JSON 数据
  reason        TEXT                     示例: LLM fallback rule fa…
  created_at    TIMESTAMP                示例: 2026-05-17 13:33:41 …

  hit_logs ~47 rows
────────────────────────────────────────────────────────────────────────
  name        type       flags         comment
  ──────────  ─────────  ────────────  ────────────────────
  id          INTEGER    PK            标识符
  rule_id     TEXT       NN            标识符
  intent      TEXT       NN            示例: security_management
  input_text  TEXT                     示例: tsf consul 怎么运…
  confidence  REAL                     示例: 0.66
  session_id  TEXT                     标识符
  elapsed_ms  INTEGER                  示例: 0
  created_at  TIMESTAMP                示例: 2026-05-17 12:23:08 …
  indexes: IDX(created_at)  IDX(intent)  IDX(rule_id)

  intent_domains ~75 rows
────────────────────────────────────────────────────────────────────────
  name        type  flags         comment
  ──────────  ────  ────────────  ────────────────────
  intent_id   TEXT  PK            标识符
  domain_tag  TEXT  PK            示例: IT/L1/facility
  indexes: UNI(intent_id,domain_tag)

  intent_rules ~29 rows
────────────────────────────────────────────────────────────────────────
  name       type  flags         comment
  ─────────  ────  ────────────  ────────────────────
  intent_id  TEXT  PK            标识符
  rule_id    TEXT  PK            标识符
  indexes: IDX(rule_id)  IDX(intent_id)  UNI(intent_id,rule_id)

  intents ~88 rows
────────────────────────────────────────────────────────────────────────
  name                 type       flags         comment
  ───────────────────  ─────────  ────────────  ────────────────────
  intent_id            TEXT       PK            标识符
  display_name         TEXT       NN            名称
  description          TEXT                     IP 地址
  required_slots       TEXT                     示例: []
  examples             TEXT                     示例: ["查看服务器状…
  created_at           TIMESTAMP                示例: 2026-05-17 12:05:33 …
  action_meta          TEXT                     示例: PERCEIVE
  domain_tag           TEXT                     示例: IT/Cross
  layer_id             TEXT                     标识符
  operation_type       TEXT                     类型
  required_slots_json  TEXT                     JSON 数据
  examples_json        TEXT                     JSON 数据
  display_name_en      TEXT                     名称
  priority             INTEGER                  示例: 0
  is_cross_layer       INTEGER                  标志位
  parent_intent_id     TEXT                     标识符
  tags_json            TEXT                     JSON 数据
  version              INTEGER                  示例: 1
  indexes: UNI(intent_id)

  role_intents
────────────────────────────────────────────────────────────────────────
  name       type  flags         comment
  ─────────  ────  ────────────  ────────────────────
  role_id    TEXT  PK            
  intent_id  TEXT  PK            
  indexes: UNI(role_id,intent_id)

  roles
────────────────────────────────────────────────────────────────────────
  name         type  flags         comment
  ───────────  ────  ────────────  ────────────────────
  role_id      TEXT  PK            
  role_name    TEXT  NN            
  parent_role  TEXT                
  description  TEXT                
  indexes: UNI(role_id)

  rule_versions ~24 rows
────────────────────────────────────────────────────────────────────────
  name            type       flags         comment
  ──────────────  ─────────  ────────────  ────────────────────
  id              INTEGER    PK            标识符
  rule_id         TEXT       NN            标识符
  version         INTEGER    NN            示例: 1
  condition_json  TEXT                     JSON 数据
  action_json     TEXT                     JSON 数据
  weight          REAL                     示例: 1
  status          TEXT                     状态
  changed_by      TEXT                     示例: llm_evolution
  change_reason   TEXT                     示例: upsert
  created_at      TIMESTAMP                示例: 2026-05-17 12:02:28 …
  indexes: IDX(rule_id)

  rules ~34 rows
────────────────────────────────────────────────────────────────────────
  name               type       flags         comment
  ─────────────────  ─────────  ────────────  ────────────────────
  rule_id            TEXT       PK            标识符
  type               TEXT                     类型
  condition_json     TEXT                     JSON 数据
  action_json        TEXT                     JSON 数据
  weight             REAL                     示例: 1
  total_activations  INTEGER                  金额/数量
  successes          INTEGER                  示例: 0
  failures           INTEGER                  示例: 0
  status             TEXT                     状态
  created_by         TEXT                     示例: seed_llm
  birth_version      INTEGER                  示例: 1
  last_used          TEXT                     
  evolution_parent   TEXT                     
  created_at         TIMESTAMP                示例: 2026-05-17 18:18:40 …
  domain_tag         TEXT                     
  action_meta        TEXT                     
  layer_id           TEXT                     标识符
  role_scope         TEXT                     
  source_dataset     TEXT                     
  indexes: UNI(rule_id)

  sessions ~66 rows
────────────────────────────────────────────────────────────────────────
  name          type       flags         comment
  ────────────  ─────────  ────────────  ────────────────────
  session_id    TEXT       PK            标识符
  context_json  TEXT                     JSON 数据
  updated_at    TIMESTAMP                时间
  indexes: UNI(session_id)

  slot_templates
────────────────────────────────────────────────────────────────────────
  name          type     flags         comment
  ────────────  ───────  ────────────  ────────────────────
  template_id   TEXT     PK            
  intent_id     TEXT     NN            
  slot_name     TEXT     NN            
  slot_type     TEXT                   
  slot_pattern  TEXT                   
  required      INTEGER                
  description   TEXT                   
  indexes: UNI(template_id)

▸ aiops-mysql  /  testdb

  iplist [InnoDB] ~12 rows 16.0KB
────────────────────────────────────────────────────────────────────────
  name         type          flags         comment
  ───────────  ────────────  ────────────  ────────────────────
  ID           int           PK            标识符
  envlabel     varchar(100)  NN            产品环境标识
  product      varchar(100)  NN            产品标识
  subproduct   varchar(100)                示例: [97 105 111 112 115 …
  hostip       varchar(100)                IP 地址
  bmcip        varchar(100)                IP 地址
  device_type  varchar(20)                 类型
  arch         varchar(20)                 示例: [88 56 54]
  card         varchar(50)                 显卡型号
  cardtype     varchar(30)                 类型
  cardnumber   int                         示例: 1
  datacenter   varchar(20)                 示例: [120 97]
  dcloc        varchar(20)                 示例: [119 116 104 111 109…
  owner        varchar(100)                机器所属
  isreg        tinyint(1)    NN            1表示纳入监控，0表示不予监控

  port [InnoDB] ~30 rows 16.0KB
────────────────────────────────────────────────────────────────────────
  name         type          flags         comment
  ───────────  ────────────  ────────────  ────────────────────
  ID           int           PK            标识符
  port         varchar(100)  NN            示例: [57 52 48 48]
  service      varchar(50)   NN            示例: [110 111 100 101 101…
  protocol     varchar(100)                协议
  description  varchar(100)                IP 地址
  isreg        tinyint(1)                  1表示注册到consul,0表示不注册

▸ openim-redis  /  db0

  _server_info  Redis 7.0.0 | memory=2.28M | total_keys=keys=389,expires=389,avg_ttl=3524978597
────────────────────────────────────────────────────────────────────────
    (no columns)

  SEQ_USER_READ:si_{hex}_{hex}:{hex} ~30 rows  30 keys, type=hash, avg memory=0.1KB  key=SEQ_USER_READ:si_{hex}_{hex}:{hex}
────────────────────────────────────────────────────────────────────────
  name   type     flags         comment
  ─────  ───────  ────────────  ────────────────────
  value  integer  NN            hash field

  CONVERSATION:{hex}:si_{hex}_{hex} ~26 rows  26 keys, type=hash, avg memory=0.6KB  key=CONVERSATION:{hex}:si_{hex}_{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  SEND_MSG_FAILED_FLAG:{hex}-{hex} ~17 rows  17 keys, type=string, avg memory=0.1KB  key=SEND_MSG_FAILED_FLAG:{hex}-{hex}
────────────────────────────────────────────────────────────────────────
  name     type      flags         comment
  ───────  ────────  ────────────  ────────────────────
  (value)  integer   NN            2
  ttl      duration  NN            4h29m29s

  SEQ_USER_READ:sg_{hex}:{hex} ~16 rows  16 keys, type=hash, avg memory=0.1KB  key=SEQ_USER_READ:sg_{hex}:{hex}
────────────────────────────────────────────────────────────────────────
  name   type     flags         comment
  ─────  ───────  ────────────  ────────────────────
  value  integer  NN            hash field

  CONVERSATION:{hex}:sg_{hex} ~16 rows  16 keys, type=hash, avg memory=0.6KB  key=CONVERSATION:{hex}:sg_{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  GROUP_MEMBER_INFO:{hex}-{hex} ~16 rows  16 keys, type=hash, avg memory=0.5KB  key=GROUP_MEMBER_INFO:{hex}-{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  SEQ_USER_MIN:si_{hex}_{hex}:{hex} ~15 rows  15 keys, type=hash, avg memory=0.1KB  key=SEQ_USER_MIN:si_{hex}_{hex}:{hex}
────────────────────────────────────────────────────────────────────────
  name   type     flags         comment
  ─────  ───────  ────────────  ────────────────────
  value  integer  NN            hash field

  MALLOC_SEQ:si_{hex}_{hex} ~15 rows  15 keys, type=hash, avg memory=0.1KB | very long TTL (8756h25m51s)  key=MALLOC_SEQ:si_{hex}_{hex}
────────────────────────────────────────────────────────────────────────
  name  type     flags         comment
  ────  ───────  ────────────  ────────────────────
  CURR  integer  NN            hash field
  LAST  integer  NN            hash field
  TIME  integer  NN            hash field

  MALLOC_SEQ:n_{hex}_{hex} ~14 rows  14 keys, type=hash, avg memory=0.1KB | very long TTL (8571h11m47s)  key=MALLOC_SEQ:n_{hex}_{hex}
────────────────────────────────────────────────────────────────────────
  name  type     flags         comment
  ────  ───────  ────────────  ────────────────────
  CURR  integer  NN            hash field
  LAST  integer  NN            hash field
  TIME  integer  NN            hash field

  SEQ_USER_MAX:si_{hex}_{hex}:{hex} ~13 rows  13 keys, type=hash, avg memory=0.1KB  key=SEQ_USER_MAX:si_{hex}_{hex}:{hex}
────────────────────────────────────────────────────────────────────────
  name   type     flags         comment
  ─────  ───────  ────────────  ────────────────────
  value  integer  NN            hash field

  SEQ_USER_MAX:sg_{hex}:{hex} ~13 rows  13 keys, type=hash, avg memory=0.1KB  key=SEQ_USER_MAX:sg_{hex}:{hex}
────────────────────────────────────────────────────────────────────────
  name   type     flags         comment
  ─────  ───────  ────────────  ────────────────────
  value  integer  NN            hash field

  SEQ_USER_MIN:sg_{hex}:{hex} ~13 rows  13 keys, type=hash, avg memory=0.1KB  key=SEQ_USER_MIN:sg_{hex}:{hex}
────────────────────────────────────────────────────────────────────────
  name   type     flags         comment
  ─────  ───────  ────────────  ────────────────────
  value  integer  NN            hash field

  UID_PID_TOKEN_STATUS:{hex}: ~13 rows  13 keys, type=hash, avg memory=0.4KB | very long TTL (1851h23m41s)  key=UID_PID_TOKEN_STATUS:{hex}:
────────────────────────────────────────────────────────────────────────
  name                                      type     flags         comment
  ────────────────────────────────────────  ───────  ────────────  ────────────────────
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey…  integer  NN            hash field

  USER_INFO:{hex} ~12 rows  12 keys, type=hash, avg memory=0.3KB  key=USER_INFO:{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  CONVERSATION_IDS:{hex} ~11 rows  11 keys, type=hash, avg memory=0.3KB  key=CONVERSATION_IDS:{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  ONLINE:{hex} ~11 rows  11 keys, type=zset, avg memory=0.1KB  key=ONLINE:{hex}
────────────────────────────────────────────────────────────────────────
  name    type     flags         comment
  ──────  ───────  ────────────  ────────────────────
  member  string   NN            
  score   float64  NN            

  FRIEND_MAX_VERSION:{hex} ~11 rows  11 keys, type=hash, avg memory=0.3KB  key=FRIEND_MAX_VERSION:{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  CONVERSATION_USER_MAX:{hex} ~11 rows  11 keys, type=hash, avg memory=0.4KB  key=CONVERSATION_USER_MAX:{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  GROUP_JOIN_MAX_VERSION:{hex} ~11 rows  11 keys, type=hash, avg memory=0.4KB  key=GROUP_JOIN_MAX_VERSION:{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  NOT_NOTIFY_CONVERSATION_IDS:{hex} ~10 rows  10 keys, type=hash, avg memory=0.1KB  key=NOT_NOTIFY_CONVERSATION_IDS:{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  JOIN_GROUPS_KEY:{hex} ~10 rows  10 keys, type=hash, avg memory=0.1KB  key=JOIN_GROUPS_KEY:{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  FRIEND_IDS:{hex} ~10 rows  10 keys, type=hash, avg memory=0.1KB  key=FRIEND_IDS:{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  MSG_CACHE:si_{hex}_{hex}:{id} ~9 rows  9 keys, type=hash, avg memory=1.0KB  key=MSG_CACHE:si_{hex}_{hex}:{id}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  PINNED_CONVERSATION_IDS:{hex} ~9 rows  9 keys, type=hash, avg memory=0.1KB  key=PINNED_CONVERSATION_IDS:{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  MSG_CACHE:sg_{hex}:{id} ~7 rows  7 keys, type=hash, avg memory=0.9KB  key=MSG_CACHE:sg_{hex}:{id}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  MSG_CACHE:si_{hex}_{hex}:5 ~7 rows  7 keys, type=hash, avg memory=1.2KB  key=MSG_CACHE:si_{hex}_{hex}:5
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  MALLOC_SEQ:n_{hex} ~4 rows  4 keys, type=hash, avg memory=0.1KB | very long TTL (8756h33m26s)  key=MALLOC_SEQ:n_{hex}
────────────────────────────────────────────────────────────────────────
  name  type     flags         comment
  ────  ───────  ────────────  ────────────────────
  TIME  integer  NN            hash field
  CURR  integer  NN            hash field
  LAST  integer  NN            hash field

  MALLOC_SEQ:sg_{hex} ~4 rows  4 keys, type=hash, avg memory=0.1KB | very long TTL (8756h25m51s)  key=MALLOC_SEQ:sg_{hex}
────────────────────────────────────────────────────────────────────────
  name  type     flags         comment
  ────  ───────  ────────────  ────────────────────
  TIME  integer  NN            hash field
  CURR  integer  NN            hash field
  LAST  integer  NN            hash field

  GROUP_MEMBER_NUM_CACHE:{hex} ~4 rows  4 keys, type=hash, avg memory=0.1KB  key=GROUP_MEMBER_NUM_CACHE:{hex}
────────────────────────────────────────────────────────────────────────
  name   type     flags         comment
  ─────  ───────  ────────────  ────────────────────
  value  integer  NN            hash field

  GROUP_INFO:{hex} ~4 rows  4 keys, type=hash, avg memory=0.6KB  key=GROUP_INFO:{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  GROUP_MEMBER_MAX_VERSION:{hex} ~4 rows  4 keys, type=hash, avg memory=0.4KB  key=GROUP_MEMBER_MAX_VERSION:{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  GROUP_ROLE_LEVEL_MEMBER_IDS:{hex}-{id} ~4 rows  4 keys, type=hash, avg memory=0.1KB  key=GROUP_ROLE_LEVEL_MEMBER_IDS:{hex}-{id}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  SEQ_USER_READ:sg_{hex}:imAdmin ~3 rows  3 keys, type=hash, avg memory=0.1KB  key=SEQ_USER_READ:sg_{hex}:imAdmin
────────────────────────────────────────────────────────────────────────
  name   type     flags         comment
  ─────  ───────  ────────────  ────────────────────
  value  integer  NN            hash field

  MSG_CACHE:sg_{hex}:1 ~3 rows  3 keys, type=hash, avg memory=3.2KB  key=MSG_CACHE:sg_{hex}:1
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  MSG_CACHE:n_{hex}_{hex}:{id} ~2 rows  2 keys, type=hash, avg memory=1.2KB  key=MSG_CACHE:n_{hex}_{hex}:{id}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  BLACK_IDS:{hex} ~2 rows  2 keys, type=hash, avg memory=0.1KB  key=BLACK_IDS:{hex}
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  CHAT_UID_TOKEN_STATUS:imAdmin ~1 rows  1 keys, type=hash, avg memory=1.7KB | very long TTL (2013h3m32s)  key=CHAT_UID_TOKEN_STATUS:imAdmin
────────────────────────────────────────────────────────────────────────
  name                                      type     flags         comment
  ────────────────────────────────────────  ───────  ────────────  ────────────────────
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey…  integer  NN            hash field
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey…  integer  NN            hash field
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey…  integer  NN            hash field
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey…  integer  NN            hash field
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey…  integer  NN            hash field
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey…  integer  NN            hash field

  MSG_CACHE:sg_{hex}:8 ~1 rows  1 keys, type=hash, avg memory=0.8KB  key=MSG_CACHE:sg_{hex}:8
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  MSG_CACHE:n_{hex}:1 ~1 rows  1 keys, type=hash, avg memory=1.4KB  key=MSG_CACHE:n_{hex}:1
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  MSG_CACHE:sg_{hex}:9 ~1 rows  1 keys, type=hash, avg memory=1.1KB  key=MSG_CACHE:sg_{hex}:9
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  CHAT_UID_TOKEN_STATUS:{hex} ~1 rows  1 keys, type=hash, avg memory=0.9KB | very long TTL (1735h9m2s)  key=CHAT_UID_TOKEN_STATUS:{hex}
────────────────────────────────────────────────────────────────────────
  name                                      type     flags         comment
  ────────────────────────────────────────  ───────  ────────────  ────────────────────
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey…  integer  NN            hash field
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey…  integer  NN            hash field
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey…  integer  NN            hash field

  OBJECT:{hex}/msg_picture_{hex}.png:minio ~1 rows  1 keys, type=hash, avg memory=0.5KB  key=OBJECT:{hex}/msg_picture_{hex}.png:minio
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  USER_INFO:imAdmin ~1 rows  1 keys, type=hash, avg memory=0.3KB  key=USER_INFO:imAdmin
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  MSG_CACHE:sg_{hex}:7 ~1 rows  1 keys, type=hash, avg memory=1.1KB  key=MSG_CACHE:sg_{hex}:7
────────────────────────────────────────────────────────────────────────
  name   type  flags         comment
  ─────  ────  ────────────  ────────────────────
  value  json  NN            hash field

  UID_PID_TOKEN_STATUS:{hex}:Web ~1 rows  1 keys, type=hash, avg memory=0.6KB | very long TTL (1735h9m2s)  key=UID_PID_TOKEN_STATUS:{hex}:Web
────────────────────────────────────────────────────────────────────────
  name                                      type     flags         comment
  ────────────────────────────────────────  ───────  ────────────  ────────────────────
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey…  integer  NN            hash field
  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.ey…  integer  NN            hash field

▸ video-pg  /  videomon

  abnormal_events
────────────────────────────────────────────────────────────────────────
  name             type                         flags         comment
  ───────────────  ───────────────────────────  ────────────  ────────────────────
  id               uuid                         PK            标识符
  camera_id        uuid                         NN            标识符
  event_time       timestamp without time zone  NN            时间
  event_type       character varying(50)        NN            类型
  description      text                         NN            IP 地址
  severity         integer                                    1-5, 5最高
  video_clip_path  character varying(500)                     IP 地址
  screenshot_path  character varying(500)                     示例: http://localhost:900…
  start_time       timestamp without time zone                时间
  end_time         timestamp without time zone                时间
  is_handled       boolean                                    标志位
  handled_by       uuid                                       示例: NULL
  handled_at       timestamp without time zone                示例: NULL
  extra_metadata   json                                       JSON 数据
  created_at       timestamp without time zone                示例: 2026-02-19 08:55:25.…
  indexes: UNI(id)  IDX(event_time)

  alert_logs
────────────────────────────────────────────────────────────────────────
  name           type                         flags         comment
  ─────────────  ───────────────────────────  ────────────  ────────────────────
  id             uuid                         PK            标识符
  event_id       uuid                         NN            标识符
  alert_time     timestamp without time zone  NN            时间
  alert_message  text                         NN            示例: 监控画面显示�…
  alert_channel  character varying(50)                      示例: system
  is_notified    boolean                                    标志位
  notified_at    timestamp without time zone                示例: 2026-02-19 08:55:25.…
  created_at     timestamp without time zone                示例: 2026-02-19 08:55:25.…
  indexes: UNI(id)

  cameras
────────────────────────────────────────────────────────────────────────
  name                type                         flags         comment
  ──────────────────  ───────────────────────────  ────────────  ────────────────────
  id                  uuid                         PK            标识符
  name                character varying(100)       NN            名称
  rtsp_url            character varying(500)       UNI NN        URL
  location            character varying(200)                     示例: 客厅
  status              integer                                    0:离线 1:在线
  config              json                                       JSON 数据
  last_heartbeat      timestamp without time zone                示例: 2026-02-19 17:05:43.…
  health_check_count  integer                                    示例: 0
  created_at          timestamp without time zone                示例: 2026-02-19 08:55:21.…
  updated_at          timestamp without time zone                时间
  indexes: UNI(id)  UNI(rtsp_url)

  reports
────────────────────────────────────────────────────────────────────────
  name         type                         flags         comment
  ───────────  ───────────────────────────  ────────────  ────────────────────
  id           uuid                         PK            标识符
  name         character varying(200)       NN            名称
  type         character varying(50)                      类型
  start_time   timestamp without time zone  NN            时间
  end_time     timestamp without time zone  NN            时间
  camera_ids   json                                       JSON 数据
  event_types  json                                       类型
  content      json                                       JSON 数据
  summary      text                                       示例: NULL
  file_path    character varying(500)                     示例: NULL
  status       character varying(20)                      状态
  progress     integer                                    示例: 0
  created_by   uuid                                       示例: NULL
  created_at   timestamp without time zone                示例: 2026-02-19 09:26:00.…
  indexes: UNI(id)

  video_descriptions
────────────────────────────────────────────────────────────────────────
  name            type                         flags         comment
  ──────────────  ───────────────────────────  ────────────  ────────────────────
  id              uuid                         PK            标识符
  camera_id       uuid                         NN            标识符
  timestamp       timestamp without time zone  NN            时间
  description     text                         NN            IP 地址
  embedding       vector(1024)                               示例: NULL
  is_abnormal     boolean                                    标志位
  frame_path      character varying(500)                     
  extra_metadata  json                                       JSON 数据
  created_at      timestamp without time zone                示例: 2026-02-19 08:55:24.…
  indexes: UNI(id)  IDX("timestamp")

▸ aiops-clickhouse  /  ai_obs

  agent_events [MergeTree]
────────────────────────────────────────────────────────────────────────
  name               type                    flags         comment
  ─────────────────  ──────────────────────  ────────────  ────────────────────
  timestamp          DateTime64(3)           PK SORT PART  
  trace_id           String                  PK SORT       
  span_id            String                  NN            
  parent_span_id     String                  NN            
  agent_id           String                  PK SORT       
  service_name       String                  NN            
  environment        LowCardinality(String)  NN            
  event_type         LowCardinality(String)  NN            
  event_name         String                  NN            
  llm_model          String                  NN            
  prompt_tokens      UInt32                  NN            
  completion_tokens  UInt32                  NN            
  total_tokens       UInt32                  NN            
  tool_name          String                  NN            
  latency_ms         UInt32                  NN            
  status             LowCardinality(String)  NN            
  payload            String                  NN            
  PARTITION BY toDate(timestamp)
  ORDER BY    agent_id, trace_id, timestamp

  mcp_server_registry [ReplacingMergeTree] ~1 rows 1.3KB  MCP Server 注册表 — Consul 的持久化备份和扩展元数据
────────────────────────────────────────────────────────────────────────
  name               type                    flags         comment
  ─────────────────  ──────────────────────  ────────────  ────────────────────
  server_name        String                  PK SORT       MCP Server 名称, 如 mcp.ops.monitor.prometheus
  domain             LowCardinality(String)  NN            工具域: monitor|messaging|logs|execution|dashboard|browser
  mcp_url            String                  NN            MCP 协议端点, 如 http://192.168.0.127:9470/mcp
  protocol           LowCardinality(String)  NN            传输协议: http|streamable-http|sse
  max_risk_level     LowCardinality(String)  NN            最高风险等级: readonly|low|high|critical
  tool_count         UInt16                  NN            当前提供的工具数量
  version            String                  NN            Server 版本号
  status             LowCardinality(String)  NN            active|inactive|draining
  health_last_check  DateTime                NN            最后一次健康检查时间
  health_status      LowCardinality(String)  NN            healthy|unhealthy|unknown
  tags               Array(String)           NN            标签: [mcp, ops, prod, grpc]
  created_at         DateTime                NN            示例: 2026-05-09 11:41:36
  updated_at         DateTime                NN            时间
  ORDER BY    server_name

  otel_traces [MergeTree]
────────────────────────────────────────────────────────────────────────
  name                type                                      flags         comment
  ──────────────────  ────────────────────────────────────────  ────────────  ────────────────────
  Timestamp           DateTime64(9)                             PK SORT PART  
  TraceId             String                                    NN            
  SpanId              String                                    NN            
  ParentSpanId        String                                    NN            
  TraceState          String                                    NN            
  SpanName            LowCardinality(String)                    PK SORT       
  SpanKind            LowCardinality(String)                    NN            
  ServiceName         LowCardinality(String)                    PK SORT       
  ResourceAttributes  Map(LowCardinality(String), String)       NN            
  ScopeName           String                                    NN            
  ScopeVersion        String                                    NN            
  SpanAttributes      Map(LowCardinality(String), String)       NN            
  Duration            UInt64                                    NN            
  StatusCode          LowCardinality(String)                    NN            
  StatusMessage       String                                    NN            
  Events.Timestamp    Array(DateTime64(9))                      NN            
  Events.Name         Array(LowCardinality(String))             NN            
  Events.Attributes   Array(Map(LowCardinality(String), Strin…  NN            
  Links.TraceId       Array(String)                             NN            
  Links.SpanId        Array(String)                             NN            
  Links.TraceState    Array(String)                             NN            
  Links.Attributes    Array(Map(LowCardinality(String), Strin…  NN            
  PARTITION BY toDate(Timestamp)
  ORDER BY    ServiceName, SpanName, toDateTime(Timestamp)

  otel_traces_trace_id_ts [MergeTree]
────────────────────────────────────────────────────────────────────────
  name     type      flags         comment
  ───────  ────────  ────────────  ────────────────────
  TraceId  String    PK SORT       
  Start    DateTime  PK SORT PART  
  End      DateTime  NN            
  PARTITION BY toDate(Start)
  ORDER BY    TraceId, Start

  tool_call_log [MergeTree]  Tool 调用观测日志 — 形成注册→调用→观测→优化闭环
────────────────────────────────────────────────────────────────────────
  name            type                    flags         comment
  ──────────────  ──────────────────────  ────────────  ────────────────────
  timestamp       DateTime64(3)           PK SORT PART  
  trace_id        String                  NN            OTel trace ID
  agent_id        LowCardinality(String)  PK SORT       调用方 Agent
  tool_id         String                  PK SORT       被调工具 ID
  tool_name       String                  NN            工具名称
  server_name     String                  NN            MCP Server
  latency_ms      UInt32                  NN            调用延迟 (ms)
  status          LowCardinality(String)  NN            success|error|timeout
  error_message   String                  NN            错误信息 (失败时)
  input_params    String                  NN            调用参数 (截断)
  output_summary  String                  NN            返回摘要 (截断)
  token_count     UInt32                  NN            工具返回内容 token 数
  PARTITION BY toDate(timestamp)
  ORDER BY    agent_id, tool_id, timestamp

  tool_registry [ReplacingMergeTree] ~61 rows 9.1KB  Tool Registry — MCP 工具的 Schema 存储和生命周期治理
────────────────────────────────────────────────────────────────────────
  name            type                    flags         comment
  ──────────────  ──────────────────────  ────────────  ────────────────────
  tool_id         String                  NN            工具唯一 ID, 如 tool_prom_query_metric
  tool_name       String                  PK SORT       工具名称, 如 mcp_prometheus_execute_query
  server_name     String                  PK SORT       所属 MCP Server, 如 mcp.ops.monitor.prometheus
  description     String                  NN            工具描述 (人工可读)
  when_to_use     String                  NN            使用场景说明 (帮助 LLM 判断何时调用)
  input_schema    String                  NN            输入 JSON Schema (完整)
  output_schema   String                  NN            输出 JSON Schema
  risk_level      LowCardinality(String)  NN            风险等级: readonly|low|high|critical
  owner           String                  NN            负责人/团队
  domain          LowCardinality(String)  NN            工具域: monitor|messaging|logs|execution|dashboard|browser
  status          LowCardinality(String)  NN            draft|reviewing|approved|published|deprecated|offline
  version         String                  NN            示例: 1.0
  tags            Array(String)           NN            标签: [prometheus, query, metrics]
  usage_count     UInt64                  NN            累计调用次数 (Observation 更新)
  last_called_at  DateTime                NN            最近一次调用时间
  avg_latency_ms  Float32                 NN            平均延迟 (ms)
  success_rate    Float32                 NN            调用成功率 (0.0-1.0)
  created_at      DateTime                NN            示例: 2026-05-09 12:50:45
  updated_at      DateTime                NN            时间
  ORDER BY    server_name, tool_name

▸ aiops-clickhouse  /  default

▸ Relationships  (3 explicit FK, 6 inferred)
  video-pg/videomon.abnormal_events(camera_id) ──FK──▶ video-pg/videomon.cameras(id)
  video-pg/videomon.alert_logs(event_id) ──FK──▶ video-pg/videomon.abnormal_events(id)
  video-pg/videomon.video_descriptions(camera_id) ──FK──▶ video-pg/videomon.cameras(id)
  aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db.conversation_steps(intent_id) ~~?~~~▶ aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db.intents(intent_id) (inferred, 85%)
  aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db.failure_log(session_id) ~~?~~~▶ aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db.sessions(session_id) (inferred, 85%)
  aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db.hit_logs(rule_id) ~~?~~~▶ aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db.rules(rule_id) (inferred, 85%)
  aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db.hit_logs(session_id) ~~?~~~▶ aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db.sessions(session_id) (inferred, 85%)
  aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db.rule_versions(rule_id) ~~?~~~▶ aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db.rules(rule_id) (inferred, 85%)
  aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db.slot_templates(intent_id) ~~?~~~▶ aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db.intents(intent_id) (inferred, 85%)

▸ Clusters (65)
  5-table cluster
    • aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/failure_log
    • aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/hit_logs
    • aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/rule_versions
    • aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/rules
    • aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/sessions
  4-table cluster
    • video-pg/videomon/abnormal_events
    • video-pg/videomon/alert_logs
    • video-pg/videomon/cameras
    • video-pg/videomon/video_descriptions
  3-table cluster
    • aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/conversation_steps
    • aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/intents
    • aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/slot_templates

▸ Issues (10)
  ℹ aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/column_docs  no timestamp column — audit trail gap
  ℹ aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/conversation_steps  no timestamp column — audit trail gap
  ℹ aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/domain_tags  no timestamp column — audit trail gap
  ℹ aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/roles  no timestamp column — audit trail gap
  ℹ aiops-sqlite//home/wwt/Downloads/aigc/proj/agents/aiops/intent-apparatus/data/rules.db/slot_templates  no timestamp column — audit trail gap
  ℹ aiops-mysql/testdb/iplist  no timestamp column — audit trail gap
  ℹ aiops-mysql/testdb/port  no timestamp column — audit trail gap
  ⚠ video-pg/videomon/abnormal_events  FK column "camera_id" has no index — full scan risk
  ⚠ video-pg/videomon/alert_logs  FK column "event_id" has no index — full scan risk
  ⚠ video-pg/videomon/video_descriptions  FK column "camera_id" has no index — full scan risk

